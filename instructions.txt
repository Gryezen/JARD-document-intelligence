to start ollama:

ollama start

(first time only — pull the model before starting)

ollama pull qwen3:8b


to clear failed data from previous version (reset sqls):

python scripts/apply_schema.py schema.sql

python scripts/apply_schema.py docs/mcp-grants.sql

to start (verify Exasol + Ollama wiring, doesn't start the app):

 C:\Users\abhin\AppData\Local\Microsoft\WindowsApps\python3.13.exe c:/Users/abhin/Documents/Code/Hackathons/ExasolProject/exasol/main.py


to start the server:

python -m api.routes

(then open http://localhost:5005)


to stop ollama:

ollama stop qwen3:8b
